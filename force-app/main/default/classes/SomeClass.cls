public class SomeClass implements Schedulable {
    public void execute(SchedulableContext ctx) {
        Account acc = new Account(Name='Jalal-Abad');
        Task task1 = new Task(Subject='There is an issue', Description='Urgent', Priority='High', WhatId=acc.Id);
    }
}